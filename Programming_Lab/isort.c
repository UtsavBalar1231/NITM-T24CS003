#include <stdio.h>
#include "common.h"

void isort(int *inputs, int size)
{
	// placehoder
}

int main(void)
{
	int inputs[8] = {3 ,2, 1, 5, 8, 7, 6, 4};
	int size = get_arr_size(inputs);
	print_arr(inputs, size);
}


